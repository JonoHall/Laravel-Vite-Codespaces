{
    "image":"mcr.microsoft.com/devcontainers/universal:2",
    "postCreateCommand": "composer create-project laravel/laravel ${localWorkspaceFolder}/tmp && mv -f ${localWorkspaceFolder}/tmp/{.,}* . && rm -rf ${localWorkspaceFolder}/tmp && mv -f ${localWorkspaceFolder}/devcontainer-postinstall.json ${localWorkspaceFolder}/.devcontainer/devcontainer.json"
}
